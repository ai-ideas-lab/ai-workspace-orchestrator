# E-Commerce Performance Demo API Documentation

## Overview
This is an optimized Express.js API for e-commerce data retrieval with performance improvements and N+1 query fixes.

## API Endpoints

### 1. Get Users with Orders
**Endpoint:** `GET /api/users-with-orders`

**Description:** Retrieves all users with their complete order information, including order items and product details. Optimized to eliminate N+1 query problems.

**Response:**
```json
[
  {
    "id": 1,
    "name": "Alice Johnson",
    "email": "alice@example.com",
    "orders": [
      {
        "id": 1,
        "order_date": "2024-01-15",
        "total_amount": 1199.98,
        "items": [
          {
            "id": 1,
            "product_id": 1,
            "quantity": 1,
            "price": 999.99,
            "product": {
              "id": 1,
              "name": "Laptop",
              "price": 999.99
            }
          }
        ]
      }
    ]
  }
]
```

### 2. Get Orders with Products
**Endpoint:** `GET /api/orders-with-products`

**Description:** Retrieves all orders with detailed product information. Uses JOIN optimization to prevent N+1 queries.

**Response:**
```json
[
  {
    "id": 1,
    "user_id": 1,
    "user_name": "Alice Johnson",
    "order_date": "2024-01-15",
    "total_amount": 1199.98,
    "items": [
      {
        "id": 1,
        "product_id": 1,
        "quantity": 1,
        "price": 999.99,
        "product": {
          "id": 1,
          "name": "Laptop",
          "price": 999.99
        }
      }
    ]
  }
]
```

### 3. Get User Statistics
**Endpoint:** `GET /api/user-stats`

**Description:** Returns comprehensive user statistics including total orders, total spending, and average order value. Features 5-minute caching for performance.

**Response:**
```json
{
  "data": [
    {
      "user": {
        "id": 1,
        "name": "Alice Johnson",
        "email": "alice@example.com"
      },
      "total_orders": 1,
      "total_spent": 1199.98,
      "average_order_value": 1199.98
    }
  ],
  "cached": false,
  "cache_age_ms": 0
}
```

### 4. Get User by ID
**Endpoint:** `GET /api/users/:id`

**Description:** Retrieves a specific user with their complete order history by user ID.

**Parameters:**
- `id` (integer): User ID

**Response:** Same structure as users-with-orders but for a single user.

### 5. Performance Test
**Endpoint:** `GET /api/performance-test`

**Description:** Runs a performance test on the optimized users-with-orders endpoint and returns execution metrics.

**Response:**
```json
{
  "execution_time_ms": 25,
  "records_processed": 20,
  "users_returned": 3,
  "optimization_note": "Uses shared helper function to eliminate code duplication"
}
```

### 6. Database Indexes
**Endpoint:** `GET /api/indexes`

**Description:** Shows all database indexes that have been created for performance optimization.

**Response:**
```json
[
  {
    "table_name": "orders",
    "index_name": "idx_orders_user_id",
    "column_name": "user_id"
  }
]
```

## Optimization Features

- **N+1 Query Fixes**: All endpoints use JOIN queries to eliminate the N+1 query problem
- **DRY Principle**: Shared helper functions to eliminate code duplication
- **Caching**: User statistics endpoint includes 5-minute TTL caching
- **Database Indexes**: Proper indexes on foreign keys for better query performance
- **Parameterized Queries**: All database queries use parameterized statements for security

## Database Schema

### Tables:
- `users`: User information (id, name, email)
- `products`: Product catalog (id, name, price, stock)
- `orders`: Order headers (id, user_id, order_date, total_amount)
- `order_items`: Order items (id, order_id, product_id, quantity, price)

### Relationships:
- Users → Orders (one-to-many)
- Orders → Order Items (one-to-many)
- Products → Order Items (one-to-many)

## Performance Notes

- All endpoints have been optimized to prevent N+1 query problems
- The application includes database indexes for common query patterns
- User statistics are cached for 5 minutes to improve response time
- Code follows DRY principle with shared helper functions

---

*Documentation auto-generated by 孔明 (Zhuge Liang) - API Documentation Generator*